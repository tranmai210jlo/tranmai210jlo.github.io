const DEFAULT_UTM={utm_source:'mxd',utm_medium:'affiliate',utm_campaign:'site'};
async function loadLinks(){let m={};try{const r=await fetch('links.json',{cache:'no-store'});if(r.ok)m=await r.json();}catch{}const l=JSON.parse(localStorage.getItem('mxd_links')||'{}');return {...m,...l}}
function ensureURL(u){try{return new URL(u)}catch{return new URL(u,location.href)}}function getSubId(){return localStorage.getItem('mxd_subid')||Date.now().toString(36)}
function buildUrl(base,params,subid){const url=ensureURL(base);Object.entries(params||{}).forEach(([k,v])=>{const val=String(v??'').replaceAll('{SUBID}',subid);if(val)url.searchParams.set(k,val)});Object.entries(DEFAULT_UTM).forEach(([k,v])=>{if(!url.searchParams.has(k))url.searchParams.set(k,v)});if(!url.searchParams.has('subid'))url.searchParams.set('subid',subid);return url.toString()}
async function wireAffiliateClicks(){const map=await loadLinks();document.querySelectorAll('[data-product-id]').forEach(el=>{el.addEventListener('click',()=>{const id=el.getAttribute('data-product-id');const it=map[id];if(!it||!it.url)return;const subid=el.getAttribute('data-subid')||getSubId();const finalUrl=buildUrl(it.url,it.params,subid);el.setAttribute('href',finalUrl);try{gtag('event','affiliate_click',{item_id:id,network:it.network||'na',subid})}catch{}},{once:true})})}
document.addEventListener('DOMContentLoaded',wireAffiliateClicks);
if(location.hash==='#admin'){localStorage.setItem('mxd_admin_gate','1');location.replace('admin.html')}
